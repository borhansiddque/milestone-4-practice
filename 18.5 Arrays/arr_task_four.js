/** 
 * 4. Checking if it's an Array
        Instructions:
        4.1 Create different variables, each containing either an array or a non-array value.
        4.2 Now use isArray to check if each variable is an array.
        4.3 Print a message to the console indicating whether each variable is an array or not.
*/

let fullName = "Borhan Siddque";
let marks = [50, 60, 70, 80];

console.log(Array.isArray(fullName));
console.log(Array.isArray(marks));
